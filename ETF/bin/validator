cd `dirname $(realpath $0)`
cd ..
. ../.venv/bin/activate &>/dev/null
python=python
test `which python3` && python=python3
test `which python` && python=python
$python -c "import core.functions as fn; fn.score($1)"
